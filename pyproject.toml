[project]
name = "dissertation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "kaggle>=2.2.4",
    "lag-llama",
    "lightgbm>=4.7.0",
    "matplotlib>=3.11.1",
    "psutil>=7.2.2",
    # pytorch-lightning 2.1.4 (pinned by lag-llama) imports pkg_resources,
    # which setuptools removed in 81.
    "setuptools<81",
    "statsforecast>=2.1.1",
]

[tool.uv.sources]
lag-llama = { git = "https://github.com/time-series-foundation-models/lag-llama.git" }
